<!--
  Main navigation bar using Bootstrap classes.
  - navbar: Main navigation container.
  - navbar-expand-lg: Expands navbar on large screens, collapses on small.
  - bg-body-tertiary: Applies a background color (Bootstrap 5 utility).
-->
<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <!--
      Brand/logo link.
      - navbar-brand: Bootstrap class for branding.
      - href="#": Placeholder link (update as needed).
    -->
    <a class="navbar-brand" href="#">Northwind Web</a>

    <!--
      Navbar toggler button:
      - Used for collapsing/expanding the navigation menu on small screens.
      - data-bs-toggle="collapse": Enables Bootstrap's collapse behavior.
      - data-bs-target="#navbarNavAltMarkup": Targets the collapsible menu div.
      - aria-controls: Identifies the element controlled by this button.
      - aria-expanded: Indicates if the menu is expanded (false by default).
      - aria-label: Provides an accessible label for screen readers.
    -->
    <button class="navbar-toggler" type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup"
            aria-expanded="false"
            aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!--
      Collapsible navigation menu.
      - collapse navbar-collapse: Bootstrap classes for collapsible content.
      - id="navbarNavAltMarkup": Must match data-bs-target of the toggler.
    -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <!--
          Navigation links using Blazor's NavLink component.
          - class="nav-link": Bootstrap styling for links.
          - href: Route to navigate to.
          - NavLink automatically applies 'active' class when route matches.
        -->
        <NavLink class="nav-link" href="/">
          Home
        </NavLink>

        <NavLink class="nav-link" href="suppliers">
          Suppliers
        </NavLink>

        <NavLink class="nav-link" href="customers">
          Customers
        </NavLink>
      </div>
    </div>
  </div>
</nav>
